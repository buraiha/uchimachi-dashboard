# AI Development Harness

## 目的

AIは、要求を満たすために既存コードを理解し、必要最小限の変更を行い、変更結果を検証してから作業を完了すること。

単にコードを書いた時点では、作業完了とはみなさない。

この `AGENTS.md` は、プロジェクト知識を網羅する文書ではなく、AIがこのリポジトリで常に従う仕事の進め方を定める入口である。

基本の流れは次のとおりとする。

```text
要求理解 → 調査 → 変更方針 → 実装 → 検証 → 必要な修正 → 最終確認 → 報告
```

## 必須ドキュメント

すべての作業で、作業開始前に以下を確認すること。

- `.ai/common.md`
- `.ai/project.md`

依頼内容に対応する開発フェーズがある場合は、あわせて `.ai/guides/` 配下の該当ファイルを確認すること。

個別タスク文書が指定されている場合は、`.ai/tasks/` 配下の指定ファイルを確認し、記載された対象範囲と受け入れ条件に従うこと。連番が前後する未指定のタスクを、自動的な実装対象として扱わない。

タスクの `Status` は次のように扱う。`proposed` は内容の検討・レビューに留め、ユーザーが実装を明示的に承認するまで実装しない。`approved` は実装を開始でき、`in-progress` は継続作業の対象とする。`completed` は現在の実装を確認し、ユーザーが再開または追加変更を明示しない限り再実装しない。ユーザーから現在の依頼で明示された指示がある場合は、その指示を優先する。

`.ai/` 配下に設計書、開発計画、個別タスク指示などが追加されている場合は、最初にファイル一覧を確認し、今回の依頼に関係するものを読む。実際に読んでいない文書を確認済みとして扱わない。

プロジェクトの仕様や設計を理解する必要がある場合は、`README.md` および `docs/` 配下の関連文書を確認する。すべてを無条件に読むのではなく、依頼に関係する文書を選ぶ。

## 指示の優先順位

指示が矛盾する場合は、次の優先順位に従う。

1. 実行環境によって与えられたシステム上およびセキュリティ上の制約
2. ユーザーからの明示的な要求
3. この `AGENTS.md`
4. `.ai/project.md` のプロジェクト固有ルール
5. `.ai/tasks/` の指定された個別タスクルール
6. `.ai/guides/` のフェーズ別ルール
7. `.ai/common.md` の共通ルール
8. 既存コード、テスト、設定、ドキュメントから読み取れる慣習

上位の指示がある場合でも、データの消失、本番環境への影響、セキュリティ上の問題が予想されるときは、実行前に対象と影響を確認すること。

## 基本原則

- 変更前に、要求、既存実装、影響範囲、検証方法を確認する。
- 要求を満たすために必要な変更だけを行う。
- ユーザーの既存変更や、要求と無関係な変更を保護する。
- 実装後は、利用可能で変更内容に関係する検証を実行する。
- 検証コマンドを決め打ちせず、マニフェスト、スクリプト、CI設定、README、既存テストから、このプロジェクトで利用可能な既存の検証手段を調査して使う。
- 確認できていない事項を、確認済みとして扱わない。
- 明示的な依頼なしに、コミット、プッシュ、デプロイ、本番操作、外部サービスへの書き込みを行わない。

## 完了条件

以下を満たした時点で作業完了とする。

1. 要求と受け入れ条件を満たしている
2. 必要な変更だけが行われている
3. 利用可能で変更内容に関係する検証が実行されている
4. 今回の変更に起因する問題が解消されている
5. 最終的な変更全体を確認している
6. 既存のユーザー変更が保護されている
7. 実行できなかった検証、既存問題、懸念事項が明示されている

「実装した」ことではなく、「要求を満たし、検証された」ことを完了条件とする。

重要な検証を実行できなかった場合や、今回の変更に起因する問題が残っている場合は、完全に検証済みの完了として扱わず、「一部未確認」または「未完了」と明示する。
